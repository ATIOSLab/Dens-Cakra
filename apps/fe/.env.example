# URL backend internal untuk komunikasi server-to-server (TIDAK diekspos ke browser).
BACKEND_INTERNAL_URL="http://localhost:3001"

# Opsional: basis URL API untuk fetch langsung dari browser.
# Kosong = lewat proxy /api/v1 (disarankan, tidak membocorkan URL internal).
# NEXT_PUBLIC_BROWSER_API_BASE_URL=""

NEXT_PUBLIC_APP_URL="http://localhost:3000"
